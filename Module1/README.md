# Module 1 — RAG-Powered AI App (MVP)
This folder contains:
- project report (report_module1.md)
- a minimal Streamlit + LangChain-style RAG app skeleton (app.py)
- requirements.txt
- example data/ folder placeholder

## How to run (local dev)
1. Create a virtualenv and install dependencies:
   ```
   python -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```
2. Populate `data/` with PDF/text files.
3. Run: `streamlit run app.py`

Note: This is a scaffold. Replace API keys and adjust vector DB settings.
